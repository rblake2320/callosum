# CLAUDE.md

See `AGENTS.md` for setup, test commands, module map, and conventions — kept
as the single source of truth so the two don't drift.

If you're being wired in as a live hemisphere (not developing the codebase),
read `PROTOCOL.md` instead.
